Program Math;
 var A:array[1..100] of integer;
 i, max:integer;
 begin
  randomize;
  for i:=1 to 100 do
  A[i]:=random(50);
  For i:=1 to 100 do
  if max<A[i] then max:=A[i];
  writeln('max',max);
  readln
  end.